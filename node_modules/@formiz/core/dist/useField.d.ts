import { UseFieldProps, UseFieldValues } from './types/field.types';
export declare const useField: ({ name, debounce, defaultValue, formatValue, onChange, required, validations, asyncValidations, keepValue, }: UseFieldProps) => UseFieldValues;
